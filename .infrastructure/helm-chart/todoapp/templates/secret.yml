apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-{{ .Values.todoapp.names.app_secret }}
  namespace: {{ .Values.todoapp.namespace.name }}
type: Opaque
data:
  {{- range $k, $v := .Values.todoapp.data }}
    {{ $k }}: {{ $v | b64enc }}
  {{- end }}