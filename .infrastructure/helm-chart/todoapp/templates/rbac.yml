apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Chart.Name }}-{{ .Values.todoapp.rbac.name }}
  namespace: {{ .Values.todoapp.namespace.name}}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Chart.Name }}-{{ .Values.todoapp.rbac.name }}-role
  namespace: {{ .Values.todoapp.namespace.name}}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs:
  - get
  - list
  - watch

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Chart.Name }}-{{ .Values.todoapp.rbac.name }}-binding
  namespace: {{ .Values.todoapp.namespace.name}}
subjects:
- kind: ServiceAccount
  name: {{ .Chart.Name }}-{{ .Values.todoapp.rbac.name }}
  namespace: {{ .Values.todoapp.namespace.name}}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Chart.Name }}-{{ .Values.todoapp.rbac.name }}-role